rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Since the app uses a custom local auth system (members list), we allow public read and write access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
